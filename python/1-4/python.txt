Python




list(range(1,10))으로 반복문 없이도 1부터9까지의 list가 생성됨 (문자열은 불가)

컴프리헨션 = for문을 활용해 여러 작업을 할 수 있음(나중에 설명 )  number_list = [number for number in range(1,6)] /py첨부파일 예시 보기

컴프리헨션을 쓸 때 []를 사용하면 list {}를 사용하면 dict아니면 set이다.
튜플은 컴프리헨션이 없다.  ()로 만들면 제너레이터 컴프리헨션이고 이건 제네레이터 객체를 반환한다.

함수 {

}
