Python




***#으로 주석을 달 수 있다.

한 라인에서 권장하는 최대 문자수는 80자, 이 길이에 코드를 다 수용할 수 없으면 \를 입력한 후 다음라인에 계속 입력 => 다음 라인 을 여전히 같은 라인으로 인식함

비교하기 if, elif(else if), else{ -------------들여쓰기 중요!
if = 만약1
elif = 만약1이 아닐 때 만약2라면
else = 만약1도 아니고 만약2도 아니라면

비교연산자 : == 같다, != 다르다, <, <=, >, >=, in 속한 원소 ex) number in list

비교연산자가 없을 때 False = None, 0, 0.0, '', [], (), {}, sest()   =>값이 없거나 0이거나 비었으면 False라는 뜻
}

while = 반복문{
중단하기 = break
건너뛰기 = continue => 반복문을 종료시키진 않고 반복문 내의 다음 루프로 건너뛸 때 사용
else = break가 되지 않았을 때 작동. ex) 스무고개 문제 끝까지 정답(break)가 나오지 않았을 경우 발동 가능
}

for = 순회하기{ -----------------------------python 예제 보기(설명하기 어려움)
자료구조가 얼마나 큰지, 어떻게 구현되었는지에 관계없이 자료구조를 순회할 수 있다.
break, continue, else 다 지원함
zip() = tuple로 반환됨
}

